<h1>Listing articles</h1>

<table>
  <tr>
    <th>Full Name</th>
    <th></th>
  </tr>

  <% @authors.each do |author| %>
    <tr>
      <td><%= author.name %></td>
      <td><%= link_to 'Show', author_path(author) %></td>
    </tr>
  <% end %>
</table>
<%= link_to 'New Author',  new_author_path%></td>